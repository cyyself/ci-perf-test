name: Use the data

on:
  workflow_run:
    workflows: [Upload data]
    types:
      - completed

jobs:
  comment-on-pr:
    if: github.event.workflow_run.event == 'pull_request' &&
        github.event.workflow_run.conclusion == 'success'
    env:
      PR_NUMBER: ${{ github.event.workflow_run.pull_requests[0].number}}
    runs-on: ubuntu-24.04
    steps:
      - name: comment with artifact data
        uses: actions/github-script@v8
        with:
          script: |
            const artifactName = 'hello-artifact';
            let allArtifacts = await github.rest.actions.listWorkflowRunArtifacts({
               owner: context.repo.owner,
               repo: context.repo.repo,
               run_id: context.payload.workflow_run.id,
            });
            let matchArtifact = allArtifacts.data.artifacts.filter((artifact) => {
              return artifact.name == artifactName;
            })[0];
            let download = await github.rest.actions.downloadArtifact({
               owner: context.repo.owner,
               repo: context.repo.repo,
               artifact_id: matchArtifact.id,
               archive_format: 'zip',
            });
            const fs = require('fs');
            const path = require('path');
            const unzipper = require('unzipper');
            const temp = '${{ runner.temp }}/artifacts';
            fs.mkdirSync(extractPath, { recursive: true });
            const buffer = Buffer.from(await downloadResponse.data.arrayBuffer());
            const stream = unzipper.Extract({ path: extractPath });
            stream.on('close', () => { console.log('Artifact extracted successfully.'); });
            stream.end(buffer);
            // read the file
            fs.readFileSync(path.join(extractPath, `${process.env.PR_NUMBER}.txt`), 'utf8');
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: process.env.PR_NUMBER,
              body: `Contents of the artifact file:\n\n\`\`\`\n${fileContent}\n\`\`\``,
            });
